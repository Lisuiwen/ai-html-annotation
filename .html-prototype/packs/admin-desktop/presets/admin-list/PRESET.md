---
id: preset.admin-list
uses: [pattern.application-shell, pattern.page-header, pattern.filter-table]
optional: [data.pagination]
---

# Admin List

Starting point for an admin list page. Provides only a business-fact-free page skeleton; generation must replace system names, menus, fields, table columns, and data with the current materials. Do not inherit example business content from the Seed.
