---
id: pattern.filter-table
requires: [data.filter-bar, data.toolbar, data.data-table]
optional: [data.pagination]
---

# Filter Table

List workspace combining filter area, toolbar, table, and optional pagination. The Pattern declares composition order only; it does not duplicate component styles, scripts, or demo data.
