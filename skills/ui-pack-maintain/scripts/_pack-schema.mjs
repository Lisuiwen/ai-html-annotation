/** Supported manifest.schemaVersion values for pack authoring and consumption. */
export const SUPPORTED_SCHEMA_VERSIONS = [1, 2];
export const SUPPORTED_SCHEMA_VERSION_SET = new Set(SUPPORTED_SCHEMA_VERSIONS);
