interface:
  display_name: "HTML Prototype Build & Review"
  short_description: "Generate, annotate, review, and deliver composable native HTML product prototypes"
  default_prompt: "Use $html-prototype-build to generate and validate a reviewable native HTML product prototype from the current materials."
